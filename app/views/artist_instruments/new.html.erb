<h1>New ArtistInstrument Page</h1>

<% if flash[:my_errors]%>
<% flash[:my_errors].each do |e|%>
<%= e %>
<% end %>
<% end %>

<%= form_for @artist_instrument do |f| %>
<%= f.label :artist_id %>
<%= f.collection_select :artist_id, @artists, :id, :name%><br>

<br>

<%= f.label :instrument_id %>
<%= f.collection_select :instrument_id, @instruments, :id, :name%>

<%= f.submit %>
<% end %>